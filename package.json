{
  "name": "cloud-notes-firebase",
  "version": "1.0.0",
  "description": "Production-ready Notes Application with Firebase Authentication and Express.js backend",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "build": "node build-config.js",
    "deploy:vercel": "npm run build && vercel --prod",
    "deploy:netlify": "npm run build && netlify deploy --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "notes",
    "firebase",
    "express",
    "authentication"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "firebase": "^10.7.1",
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
